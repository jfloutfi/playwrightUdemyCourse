Playwright wait till elements are visible
Playwright wait till elements are stable => elements can be come visible but might not be fully loaded
=> before it performs some action on it

for some actions it can wait till some events related to said element complete

all the above can be found in documentation

cross browser compatible

multiplatform as well: win, mac, linux...

multi linguale flexibility: can use, JS/TS, Java, python, C#... etc

Inside that directory, you can run several commands:

  npx playwright test
    Runs the end-to-end tests.

  npx playwright test --ui
    Starts the interactive UI mode.

  npx playwright test --project=chromium
    Runs the tests only on Desktop Chrome.

  npx playwright test example
    Runs the tests in a specific file.

  npx playwright test --debug
    Runs the tests in debug mode.

  npx playwright codegen
    Auto generate tests with Codegen.

We suggest that you begin by typing:

    npx playwright test

And check out the following files:
  - ./tests/example.spec.ts - Example end-to-end test
  - ./playwright.config.ts - Playwright Test configuration

Visit https://playwright.dev/docs/intro for more information

For this course:
----------------
go to https://rahulshettyacademy.com/practice
to find practice resources

Await:
------
with playwright, we will need to use "await"
it is a bit closer to how javascript executes than Cypress, and does not have its own DSL schemes and so one,
so log term, it might be simpler to deal with that cypress, especially when it comes to commands, since in this case, they are just regular functions

when using "await", you need to make sure to designed the function as "async"

Playwright favors:

* Explicit flow
* No hidden magic
* Better debugging

Test and fixtures
-----------------
test('first playwright test', async ({browser}) => {}
browser is a fixture that availabe to all playwright tests
we must wrap it in {} else it will be treated like som regular string variable

test('first playwright test', async ({browser}) => {
    // chrome - plugings, cookies... etc
    // newContext => opens a fresh browser instance
    // we can use this method to inject cookies for example, basically configure the browser as it launches
    // this can help establish sessions the same it is done in cypress so avoid re-logins
    const context = await browser.newContext();
    const page = await context.newPage();
    await page.goto('https://somehost.com');
});

we can also use ({browser, page}) or just ({page})
using "page", playwright will create for us default browser, context... etc

playwright.config.ts
--------------------
config file, can also be used to overwrite defaults
For example

export default defineConfig({
  testDir: './tests',
  timeout: 40 * 1000, => timeout per step in test
  expect: { => configuration for "expect" object, which is used for assertions
    timeout: 50 * 1000 => timeout for assertions
  },
  use: {
    browserName: 'chromium',
    headless: false => always the browser that comes with playwright
  }
});

Only and Skip
-------------
we can use test.only() and test.skip()

unlike cypress, here we use .locator()
locator is alwasy scoped to the object before it
page.locator('parent').locator('child')
best to use CSS selectors here
same CSS selector techniques and syntax as cypress like "#" for id, "." for class, also the element type (tag name) can be included, like "button#loginButton"

await page.locator('[style*="block"]').textContent();
=> extracts text content found in element matched via locator

Consider:
1- const text:string|null = await page.locator('[style*="block"]').textContent();
   expect(text).toContain('Incorrect');

2- await expect(page.locator('[style*="block"]')).toContainText('Incorrect');

3- expect(await page.locator('[style*="block"]')).toContainText('Incorrect');

The correct one is "2", bcz toContainText() is the method that returns the promise
in "1", the value might end up as null and there will be no retries, in this case, these happen on "expect"
"3" is a bug, locator does not return a promise

text fields:
best practice to clear a text field in playwright is to use .fill('') though .fill() auto clears before filling data
in case in the test, you need to append chars to already types one, use pressSequentially()

When dealing with locators that detect multiple elements we have:
.first()
.last()
.nth(x) => x is the index of an array: 0 -> nth


